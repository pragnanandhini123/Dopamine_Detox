import { Youtube, Instagram, Twitter, MessageSquare } from 'lucide-react';
import type { LucideIcon } from 'lucide-react';

export const appIcons: { [key: string]: LucideIcon } = {
  Instagram,
  Youtube,
  Twitter,
  MessageSquare,
};
